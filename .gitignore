node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
# expo router
expo-env.d.ts

# firebase/supabase/vexo
.env
.env.local
.env.*.local

ios
android

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# Session scratch docs (continuity notes between Claude sessions)
HANDOFF.md
CONTINUE-PROMPT.md

# Internal planning, commercial strategy and security material — never publish.
# These stay on disk (they are the working record) but the repo is public, so
# they must not be tracked. Untracked 2026-07-25; still readable in history
# before that commit.
TODO.md
notes/
docs/specs/
docs/momentum-engine-v2.md
docs/pricing-2026-07-launch.md
docs/android-launch/
docs/oc3/

# Account-teardown audit logs (contain deleted-user PII — never commit)
scripts/admin/logs/
# >>> security-audit 2026-06-13 (added by Claude) >>>
.env
.env.*
!.env.example
!.env.sample
*.p8
*.pem
*.key
# Google Play submit credential (EAS submit) — service account JSON, never commit
google-play-service-account.json
*-accessKeys.csv
*accessKeys*.csv
*service_account*.json
*serviceAccount*.json
*.har
secrets/
.secrets-quarantine/
# <<< security-audit 2026-06-13 <<<

# Store build artifacts (EAS output lives on expo.dev; 100MB+ binaries never belong in git)
*.aab
*.apk
*.ipa
